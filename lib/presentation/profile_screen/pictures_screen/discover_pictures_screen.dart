import 'package:flutter/material.dart';

class DiscoverPicturesScreen extends StatelessWidget{
  const DiscoverPicturesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text("Discover pictures here.")
      ],
    );
  }
}