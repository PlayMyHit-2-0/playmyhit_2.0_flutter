import 'package:flutter/material.dart';

class FriendsPicturesScreen extends StatelessWidget{
  const FriendsPicturesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text("Friends pictures here.")
      ]
    );
  }

}